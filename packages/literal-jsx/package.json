{
  "name": "literal-jsx",
  "version": "0.1.1",
  "private": false,
  "description": "An example implementation of a Literal JSX parser (see https://literal-jsx.org/)",
  "main": "./lib/index.js",
  "author": "Kelley van Evert <hello@klve.nl>",
  "homepage": "https://literal-jsx.org/",
  "license": "MIT",
  "keywords": [
    "jsx",
    "markup",
    "json"
  ],
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@babel/cli": "^7.4.4",
    "arcsecond": "^1.3.0"
  },
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "babel-jest": "^24.8.0",
    "babel-plugin-add-module-exports": "^1.0.2",
    "babel-preset-minify": "^0.5.0",
    "cross-env": "^5.2.0",
    "jest": "^24.8.0",
    "rimraf": "^2.6.3"
  },
  "scripts": {
    "clean": "rimraf lib",
    "test": "jest",
    "build": "cross-env BABEL_ENV=production babel src --out-dir lib",
    "prepare": "yarn run clean && yarn run test && yarn run build"
  }
}
